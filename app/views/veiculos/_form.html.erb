<%= form_with(model: veiculo, local: true) do |form| %>
  <% if veiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(veiculo.errors.count, "error") %> seu veiculo não pode ser salvo pois:</h2>

      <ul>
        <% veiculo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= form.label :placa %>
    <%= form.text_field :placa,maxlength:7 %>
  </div>

  <div class="field">
    <%= form.label :renavam %>
    <%= form.text_field :renavam, maxlength: 11 %>
  </div>

    <div class="field">
      <%= form.label :proprietario %>
      <%= collection_select(:veiculo, :proprietario_id, Proprietario.all , :id, :nome) %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
