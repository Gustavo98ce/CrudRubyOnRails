<%= form_with(model: proprietario, local: true) do |form| %>
  <% if proprietario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proprietario.errors.count, "error") %> prohibited this proprietario from being saved:</h2>

      <ul>
        <% proprietario.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cpf_cnpj %>
    <%= form.text_field :cpf_cnpj %>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :endereco %>
    <%= form.text_field :endereco %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
